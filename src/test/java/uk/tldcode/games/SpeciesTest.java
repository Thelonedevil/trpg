package uk.tldcode.games;

import org.junit.Test;
import uk.tldcode.games.character.Species;
import static org.junit.Assert.assertEquals;

public class SpeciesTest {
    @Test
    public void speciesTest() {
        /*assertEquals(Species.registered.get("DRAGONBORN").name, "Dragonborn");
        assertEquals(Species.registered.get("DROW").name, "Drow");
        assertEquals(Species.registered.get("DWARF").name, "Dwarf");
        assertEquals(Species.registered.get("ELF").name, "Elf");
        assertEquals(Species.registered.get("GNOME").name, "Gnome");
        assertEquals(Species.registered.get("ORC").name, "Orc");
        assertEquals(Species.registered.get("HUMAN").name, "Human");
        assertEquals(Species.registered.get("DEMON").name, "Demon");
        assertEquals(Species.registered.get("HALFLING").name, "Halfling");*/
    }
}
